<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' && title ? title + ' - ' : '' %>SportsAmigo</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Simple Dashboard CSS -->
    <link rel="stylesheet" href="/css/simple-dashboard.css">
    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        /* 🎯 SIMPLE SIDEBAR & LAYOUT - CLEAN WHITE & RED */
        body {
            font-family: 'Segoe UI', system-ui, sans-serif;
            background: #f8f9fa;
            margin: 0;
            padding: 0;
        }
        
        /* 🎨 SIMPLE SIDEBAR */
        .sidebar {
            position: fixed;
            top: 0;
            left: 0;
            height: 100vh;
            width: 280px;
            background: #ffffff;
            border-right: 2px solid #dc3545;
            padding: 0;
            z-index: 1000;
            transform: translateX(0);
            transition: transform 0.3s ease;
        }
        
        .sidebar.collapsed {
            transform: translateX(-280px);
        }
        
        /* ✨ SIMPLE SIDEBAR HEADER */
        .sidebar-header {
            padding: 2rem 1.5rem;
            border-bottom: 1px solid #dee2e6;
            text-align: center;
            background: #ffffff;
        }
        
        .sidebar-header .logo {
            color: #dc3545;
            font-size: 1.6rem;
            font-weight: 700;
            text-decoration: none;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .sidebar-header .logo:hover {
            color: #c82333;
        }
        
        .sidebar-header .logo i {
            margin-right: 0.5rem;
            font-size: 2rem;
            color: #dc3545;
        }
        
        /* 👤 SIMPLE USER INFO */
        .user-info {
            padding: 1.5rem;
            border-bottom: 1px solid #dee2e6;
            color: #212529;
            text-align: center;
            background: #f8f9fa;
        }
        
        .user-info .user-avatar {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: #dc3545;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1rem;
            font-size: 1.8rem;
            color: #ffffff;
        }
        
        .user-info .user-name {
            font-weight: 600;
            font-size: 1.1rem;
            margin-bottom: 0.5rem;
            color: #212529;
        }
        
        .user-info .user-role {
            font-size: 0.875rem;
            color: #dc3545;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-weight: 500;
        }
        
        /* 🧭 SIMPLE NAVIGATION MENU */
        .sidebar-nav {
            list-style: none;
            padding: 1rem 0;
            margin: 0;
        }
        
        .sidebar-nav li {
            margin: 0;
        }
        
        .sidebar-nav a {
            display: flex;
            align-items: center;
            padding: 1rem 1.5rem;
            color: #6c757d;
            text-decoration: none;
            transition: all 0.2s ease;
            border-left: 3px solid transparent;
        }
        
        .sidebar-nav a:hover {
            background: #f8f9fa;
            color: #dc3545;
            border-left-color: #dc3545;
        }
        
        .sidebar-nav a.active {
            background: #f8f9fa;
            color: #dc3545;
            border-left-color: #dc3545;
            font-weight: 600;
        }
        
        .sidebar-nav a i {
            margin-right: 1rem;
            font-size: 1.1rem;
            width: 20px;
            text-align: center;
        }
        
        /* 🚪 SIMPLE LOGOUT SECTION */
        .logout-section {
            position: absolute;
            bottom: 1rem;
            left: 0;
            right: 0;
            padding: 0 1.5rem;
        }
        
        .logout-section a {
            display: flex;
            align-items: center;
            padding: 1rem;
            color: #6c757d;
            text-decoration: none;
            border: 1px solid #dee2e6;
            border-radius: 4px;
            transition: all 0.2s ease;
            background: #ffffff;
        }
        
        .logout-section a:hover {
            background: #dc3545;
            color: #ffffff;
            border-color: #dc3545;
        }
        
        .logout-section a i {
            margin-right: 1rem;
            font-size: 1rem;
        }
        
        /* 📱 SIMPLE MAIN CONTENT */
        .main-content {
            margin-left: 280px;
            min-height: 100vh;
            transition: margin-left 0.3s ease;
        }
        
        .main-content.full-width {
            margin-left: 0;
        }
        
        /* 🎯 SIMPLE TOP HEADER */
        .top-header {
            background: #ffffff;
            border-bottom: 1px solid #dee2e6;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 999;
        }
        
        .top-header h4 {
            margin: 0;
            font-weight: 600;
            color: #dc3545;
        }
        
        .toggle-sidebar {
            background: #dc3545;
            border: none;
            font-size: 1.1rem;
            color: #ffffff;
            cursor: pointer;
            padding: 0.5rem;
            border-radius: 4px;
            transition: background 0.2s ease;
        }
        
        .toggle-sidebar:hover {
            background: #c82333;
        }
        
        /* 📦 SIMPLE CONTENT AREA */
        .content-area {
            padding: 0;
        }
        
        /* 📱 RESPONSIVE DESIGN */
        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-280px);
            }
            
            .sidebar.show {
                transform: translateX(0);
            }
            
            .main-content {
                margin-left: 0;
            }
            
            .top-header {
                padding: 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar" id="sidebar">
        <!-- Logo -->
        <div class="sidebar-header">
            <a href="/" class="logo">
                <i class="fas fa-football-ball"></i>
                SportsAmigo
            </a>
        </div>
        
        <!-- User Info -->
        <div class="user-info">
            <div class="user-avatar">
                <% if (typeof user !== 'undefined' && user && user.profile_image) { %>
                    <img src="<%= user.profile_image %>" alt="Avatar" style="width: 100%; height: 100%; border-radius: 50%; object-fit: cover;">
                <% } else { %>
                    <i class="fas fa-user"></i>
                <% } %>
            </div>
            <div class="user-name"><%= typeof user !== 'undefined' && user ? (user.first_name + ' ' + user.last_name) : 'User' %></div>
            <div class="user-role"><%= typeof user !== 'undefined' && user ? user.role : 'guest' %></div>
        </div>
        
        <!-- Navigation Menu -->
        <ul class="sidebar-nav">
            <% if (typeof user !== 'undefined' && user && user.role === 'organizer') { %>
                <li><a href="/organizer/dashboard" class="<%= path === '/organizer' || path === '/organizer/dashboard' ? 'active' : '' %>">
                    <i class="fas fa-tachometer-alt"></i> Dashboard
                </a></li>
                <li><a href="/organizer/events" class="<%= path === '/organizer/events' || path === '/organizer/manage-events' ? 'active' : '' %>">
                    <i class="fas fa-calendar-alt"></i> My Events
                </a></li>
                <li><a href="/organizer/create-event" class="<%= path === '/organizer/create-event' ? 'active' : '' %>">
                    <i class="fas fa-plus-circle"></i> Create Event
                </a></li>
                <li><a href="/organizer/reports" class="<%= path === '/organizer/reports' ? 'active' : '' %>">
                    <i class="fas fa-chart-bar"></i> Reports
                </a></li>
                <li><a href="/organizer/profile" class="<%= path === '/organizer/profile' ? 'active' : '' %>">
                    <i class="fas fa-user-circle"></i> Profile
                </a></li>
            <% } else if (typeof user !== 'undefined' && user && user.role === 'player') { %>
                <li><a href="/player/dashboard" class="<%= path === '/player/dashboard' ? 'active' : '' %>">
                    <i class="fas fa-tachometer-alt"></i> Dashboard
                </a></li>
                <li><a href="/player/my-events" class="<%= path === '/player/my-events' ? 'active' : '' %>">
                    <i class="fas fa-calendar-alt"></i> My Events
                </a></li>
                <li><a href="/player/my-teams" class="<%= path === '/player/my-teams' ? 'active' : '' %>">
                    <i class="fas fa-users"></i> My Teams
                </a></li>
                <li><a href="/player/browse-events" class="<%= path === '/player/browse-events' ? 'active' : '' %>">
                    <i class="fas fa-search"></i> Browse Events
                </a></li>
                <li><a href="/player/browse-teams" class="<%= path === '/player/browse-teams' ? 'active' : '' %>">
                    <i class="fas fa-user-friends"></i> Browse Teams
                </a></li>
                <li><a href="/player/profile" class="<%= path === '/player/profile' ? 'active' : '' %>">
                    <i class="fas fa-user-circle"></i> Profile
                </a></li>
            <% } else if (typeof user !== 'undefined' && user && user.role === 'manager') { %>
                <li><a href="/manager/dashboard" class="<%= path === '/manager/dashboard' ? 'active' : '' %>">
                    <i class="fas fa-tachometer-alt"></i> Dashboard
                </a></li>
                <li><a href="/manager/my-events" class="<%= path === '/manager/my-events' ? 'active' : '' %>">
                    <i class="fas fa-calendar-alt"></i> Team Events
                </a></li>
                <li><a href="/manager/my-teams" class="<%= path === '/manager/my-teams' ? 'active' : '' %>">
                    <i class="fas fa-users"></i> My Teams
                </a></li>
                <li><a href="/manager/create-team" class="<%= path === '/manager/create-team' ? 'active' : '' %>">
                    <i class="fas fa-plus-circle"></i> Create Team
                </a></li>
                <li><a href="/manager/browse-events" class="<%= path === '/manager/browse-events' ? 'active' : '' %>">
                    <i class="fas fa-search"></i> Browse Events
                </a></li>
                <li><a href="/manager/profile" class="<%= path === '/manager/profile' ? 'active' : '' %>">
                    <i class="fas fa-user-circle"></i> Profile
                </a></li>
            <% } %>
        </ul>
        
        <!-- Logout Section -->
        <div class="logout-section">
            <a href="/logout">
                <i class="fas fa-sign-out-alt"></i>
                Logout
            </a>
        </div>
    </div>
    
    <!-- Main Content -->
    <div class="main-content" id="mainContent">
        <!-- Top Header -->
        <div class="top-header">
            <div class="d-flex align-items-center">
                <button class="toggle-sidebar" id="toggleSidebar">
                    <i class="fas fa-bars"></i>
                </button>
                <h4 class="mb-0 ms-3"><%= typeof title !== 'undefined' && title ? title : 'Dashboard' %></h4>
            </div>
            <div class="d-flex align-items-center">
                <span class="me-3">Welcome, <%= typeof user !== 'undefined' && user && user.first_name ? user.first_name : 'User' %>!</span>
                <div class="dropdown">
                    <button class="btn btn-outline-secondary btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown">
                        <i class="fas fa-user"></i>
                    </button>
                    <ul class="dropdown-menu">
                        <% if (typeof user !== 'undefined' && user && user.role) { %>
                            <li><a class="dropdown-item" href="/<%= user.role %>/profile">Profile</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/logout">Logout</a></li>
                        <% } else { %>
                            <li><a class="dropdown-item" href="/login">Login</a></li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </div>
        
        <!-- Content Area -->
        <div class="content-area">
            <%- body %>
        </div>
    </div>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Custom JS -->
    <script src="/js/form-validation.js"></script>
    
    <script>
        // Sidebar toggle functionality
        document.getElementById('toggleSidebar').addEventListener('click', function() {
            const sidebar = document.getElementById('sidebar');
            const mainContent = document.getElementById('mainContent');
            
            sidebar.classList.toggle('collapsed');
            mainContent.classList.toggle('full-width');
        });
        
        // Mobile responsive sidebar
        function handleResize() {
            const sidebar = document.getElementById('sidebar');
            const mainContent = document.getElementById('mainContent');
            
            if (window.innerWidth <= 768) {
                sidebar.classList.add('collapsed');
                mainContent.classList.add('full-width');
            } else {
                sidebar.classList.remove('collapsed');
                mainContent.classList.remove('full-width');
            }
        }
        
        window.addEventListener('resize', handleResize);
        handleResize(); // Call on load
        
        // Smooth scrolling for navigation
        document.querySelectorAll('.sidebar-nav a').forEach(link => {
            link.addEventListener('click', function(e) {
                // Add loading animation or transition if needed
                this.style.transform = 'scale(0.95)';
                setTimeout(() => {
                    this.style.transform = 'scale(1)';
                }, 150);
            });
        });
    </script>
    
    <!-- AJAX Functions for Dashboard Interactivity -->
    <script src="/js/ajax-functions.js"></script>
</body>
</html>